============
Installation
============

Requirements
============

.. todo::

   Document OS versions, ROS 2 distribution, compiler/toolchain requirements,
   Rust requirements, GPU requirements, and simulator dependencies.

Workspace Setup
===============

.. todo::

   Add clone, submodule, dependency, and environment setup commands.

Build
=====

.. todo::

   Add the canonical build command for the workspace and package-specific build
   examples.

Docker
======

.. todo::

   Document the Docker image build and headless validation command.
